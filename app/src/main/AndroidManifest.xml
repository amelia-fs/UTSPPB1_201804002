<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uts_amel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".intenexplicit_mahasiswa"></activity>
        <activity android:name=".inputmahasiswa" />
        <activity android:name=".intenexplicit_dosen" />
        <activity android:name=".inputdosen" />
        <activity android:name=".item_mahasiswa" />
        <activity android:name=".remahasiswa" />
        <activity android:name=".klikkirim" />
        <activity android:name=".daftar" />
        <activity android:name=".item_dosen" />
        <activity android:name=".redosen" />
        <activity android:name=".beranda" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>